# セキュリティモデル

このツールが何を保証し、何を保証しないかを説明します。導入前に一読してください。

## 権限モデル

このマクロは Excel を実行しているユーザーと同じ権限で動作します。VBA マクロに、Excel が持つ以上の特別な権限はありません。管理者権限で Excel を実行しない限り、管理者権限での操作はできません。

## 実行する操作と実行しない操作

対応する操作は次の4種類だけです。

- ファイル名の変更
- 走査ルート内でのファイル移動
- 新規フォルダの作成
- ファイル・空フォルダのゴミ箱への移動

次は明示的に拒否します。

- 完全削除（ゴミ箱を経由しない削除）
- 新規ファイルの作成
- フォルダの名前変更・移動
- 非空フォルダの削除
- 走査ルート外へのファイル移動
- 既存ファイル・フォルダへの上書き

## ロールバックの限界

- 名前変更・移動・新規フォルダ作成は、削除フェーズ開始前の失敗であれば逆順ロールバックを試みます。
- ファイル削除（ゴミ箱への移動）を開始した後は、操作全体を atomic な成功として扱いません。ゴミ箱への移動そのものが失敗した場合、それ以前の操作の自動復元は保証されません。
- 実行結果は `実行ログ` シートへ append-only で記録され、成功・失敗・未実行に加えて `possible-permanent-delete`（完全削除の疑いがある状態）、`recycle-verification-contaminated`（ゴミ箱内の照合が汚染された状態）を記録します。

## 実行前検査

操作の実行直前に、次をすべて再検査します。いずれかに失敗した場合、その操作は実行されません。

- 走査ルートおよび対象の実在性
- reparse point（シンボリックリンク・ジャンクション）経由でのルート逸脱
- 移動先の衝突（既存ファイル・フォルダとの重複）
- 走査後に対象が変化していないか（stale 検出）
- 編集可能列に数式が入力されていないか（`HasFormula` 検査）

外部由来のファイル名・パスはすべて文字列としてワークシートへ書き込まれるため、`=`、`+`、`-`、`@` で始まる名前が数式として実行されることはありません。

## ゴミ箱操作の境界

- 固定ローカルボリューム（`DRIVE_FIXED`）上の対象だけを扱い、ネットワークドライブ（UNC パス）は拒否します。
- ボリューム GUID を確認し、走査時と実行時で対象ボリュームが変化していないことを確認します。
- レジストリの `NukeOnDelete` ポリシー（ゴミ箱を使わず完全削除する設定）が有効な場合、その対象への操作は拒否します。**ゴミ箱が使えない場合に完全削除へフォールバックすることはありません。**
- ゴミ箱の容量（`MaxCapacity` / `RecycleBinSize`）に対する安全マージンを見積もり、超過が見込まれる場合は警告します。
- 削除前後でゴミ箱のスナップショットを取得し、対象アイテムの元パス・種別・サイズ・更新日時を照合します。既存の利用者アイテムを保持し、他アイテムとの混同を検出します。

## ネットワーク・外部プロセス

このツールは外部ネットワークへ通信しません。ファイルシステム操作（Win32 API 経由の列挙、Shell API 経由のゴミ箱移動）以外の外部プロセス起動は行いません。

## プライバシー

`ツリー` シートの走査結果および `実行ログ` シートには、対象フォルダの絶対パスがそのまま記録されます。ブックを保存した場合、そのファイルには利用者の PC 上のフォルダ構成・ファイル名が含まれます。**使用済みのブックを第三者と共有したり、Issue に添付したりしないでください。**

このツール自体はテレメトリを送信しません。

## テストビルドと配布ビルドの境界

配布される `.xlsm` は `#Const TEST_BUILD = False` でビルドされたビルド候補（release candidate）です。テスト専用の procedure（`RunScanTest` など、名前が `Test` で終わるものと `ProbeReentry` / `RunStringSafetyProbe`）は、ソースコード上は `#If TEST_BUILD Then ... #End If` で囲まれており、release candidate では実際にコンパイルされず、VBA プロジェクト内に存在しません。

配布物のビルド時には、次の3点を同一の Excel セッションで確認します。

1. 引数なしの production entrypoint（例: `GetScanSnapshotCount`）が正常値を返すこと
2. 存在しないダミーマクロを呼び出すと「マクロが定義されていない」ことを示すエラーが返ること
3. テスト専用 procedure をすべて呼び出しても、2 と同じ「マクロが定義されていない」エラーが返ること（= 実際にコンパイルされていないことの直接証拠）

## 安全性検査の検証範囲

上記「実行前検査」「ゴミ箱操作の境界」で挙げた拒否動作のうち、次の3項目は配布ビルドごとに、実際の Excel 上で通常の操作画面（`事前確認`）を経由した動的テストで検証しています。テスト専用 procedure を使わず、利用者と同じ実行経路を通します。

- 走査ルート外への移動の拒否
- reparse point 経由でルートを逸脱する移動先の拒否
- 移動先が既存ファイル・フォルダと衝突する場合の拒否（上書きしないこと）

一方、**「ゴミ箱が使えない場合に完全削除へフォールバックしない」ことについては、動的な実地検証を行っていません。** この動作は `NoRecycleFiles` / `NukeOnDelete` を検査して削除 API の呼び出し前に拒否するソースコード上のガードとして実装されており、配布される `.xlsm` に埋め込まれた VBA が公開ソースと一致することはハッシュで確認していますが、ゴミ箱を実際に無効化した環境で動作を確認したわけではありません。

この点を重視する場合は、`src/modRecycleBin.bas` の `ResolveRecycleVolumeConfig` を確認したうえで、バックアップ済みのテストデータを使ってご自身の環境で確認してください。

## 実測性能の範囲

Recycle Bin 関連の性能実測には、次の既知の制限があります。

- Shell `ExtendedProperty` の取得レイテンシは、少数の実 Recycle Bin アイテムでのみ測定しており、10,000 件・50,000 件規模での実測値ではありません。
- 削除件数 1,001〜10,000 件の所要時間は見積もりを提供せず、「見積不能」と表示します。大量削除の所要時間を保証するものではありません。

## 署名についての注意

配布物は署名なし（unsigned）です。署名は将来追加される可能性がありますが、署名済みであっても、それは発行者の同一性と配布後の改変の有無を確認する材料であり、マクロの動作そのものの安全性を保証するものではありません。ソースコードとハッシュ値を自身で確認したうえで利用してください。
